VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
# Enable only after Organizations is turned on for the Clerk instance.
VITE_CLERK_ORGANIZATIONS_ENABLED=false

# Sites Worker only (never expose these as VITE_* variables).
BACKEND_ORIGIN=http://localhost:8000
BACKEND_PROXY_SECRET=replace-with-a-long-random-secret

# Production:
# VITE_API_BASE_URL=https://your-railway-or-fly-backend.example.com/api/v1
