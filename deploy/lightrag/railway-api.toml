[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/lightrag/Dockerfile"

[deploy]
startCommand = "uvicorn lightrag_integration.runtime:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/live"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
